# Keep repo-local CLIs like `expo` and `prettier` on PATH after `direnv allow`.
PATH_add node_modules/.bin

# Reload when dependency metadata or local overrides change.
watch_file package.json package-lock.json
watch_file .envrc.local

# Optional maintainer-only overrides live here and stay untracked.
source_env_if_exists .envrc.local
