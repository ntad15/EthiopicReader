# Seasonal Source

Put seasonal slot documents here.

These files define named seasonal slots and their conditional rules. Base documents should refer to those slots with `seasonal-ref` nodes instead of embedding conditional logic on generic text nodes.
